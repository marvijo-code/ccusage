[package]
name = "ccusage-adapter-common"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
ccusage-core = { path = "../ccusage-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
ccusage-test-support = { path = "../ccusage-test-support" }
